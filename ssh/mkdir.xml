<project name="SSHTest" default="init">
	<property file="build.properties"/>
	
	<target name="init">
		<sshexec host="${host}"
			username="${user}"
			password="${password}"
			command="mkdir /var/www/html/test/test1/"
		/>
	</target>
</project>